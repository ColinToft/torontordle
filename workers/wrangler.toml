name = "torontordle-stats"
main = "src/index.ts"
compatibility_date = "2026-06-09"
workers_dev = true

# Served at https://torontordle-stats.torontordle.workers.dev. (A custom domain
# like api.torontordle.com would require the worker to live in the same account
# as the torontordle.com zone; this worker + its D1 are in the cwt1078 account.)

[[d1_databases]]
binding = "DB"
database_name = "torontordle-stats"
database_id = "f1083779-0d6b-48c5-a5c1-59fc19126832"
